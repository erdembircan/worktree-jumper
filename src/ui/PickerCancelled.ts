/**
 * The sentinel returned by {@link WorktreePicker.pick} and
 * {@link Confirmer.confirm} when the user cancels (Esc/Ctrl-C) rather
 * than answering.
 */
export const PICKER_CANCELLED = Symbol('picker-cancelled');
